<Window x:Class="Carshering.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Carshering"
        mc:Ignorable="d"
        Title="AdminWindow" Height="450" Width="800" Background="#C7BBEE">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" />
                <DataGridTextColumn Header="Логин" />
                <DataGridTextColumn Header="Пароль" />
                <DataGridTextColumn Header="Роль" />
                <DataGridTextColumn Header="Последний вход" />
                <DataGridTextColumn Header="Заблокирован" />
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnAdd" Grid.Row="2" Margin="400,350,250,0" Content="добавить пользователя" Padding="2" Background="#F4A4D1" Click="btnAdd_Click"  />
        <Button x:Name="btnDel" Grid.Row="2" Margin="580,350,10,0" Content="удалить пользователя" Padding="2" Background="#F4A4D1" />
        <Button x:Name="btnEdit" Grid.Row="2" Margin="220,350,420,0" Content="редактировать" Padding="2" Background="#F4A4D1"/>
        <Button x:Name="btnBlock_unblock" Grid.Row="2" Margin="10,350,600,0" Content="заблокировать/разблокировать" Padding="2" Background="#F4A4D1"/>

    </Grid>
</Window>
